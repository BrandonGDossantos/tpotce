# T-Pot (Standard)
# For docker-compose ...
version: '2.1'
services:

# Filebeat service
  filebeat: 
    container_name: filebeat
    restart: always
    image: "docker.elastic.co/beats/filebeat:6.4.2"